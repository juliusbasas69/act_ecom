export interface Category {
  encryptedId: string;
  categoryCode: string;
  categoryName: string;
  description: string;
  status: string;
  color: string;
  icon: string;
  createdAt: string;
  updatedAt: string;
}
