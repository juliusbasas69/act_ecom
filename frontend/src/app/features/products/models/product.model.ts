export interface Product {
  encryptedId: string;
  productCode: string;
  productName: string;
  description: string;
  category: string;
  price: number;
  quantity: number;
  createdAt: string;
  updatedAt: string;
}
